# fromBoolToBool
def void main() {
    bool initial1 = true;
    bool initial2 = false;

    bool new1 = to_bool(initial1);
    bool new2 = to_bool(initial2);

    print(to_string(new1)+" "+to_string(new2));
}